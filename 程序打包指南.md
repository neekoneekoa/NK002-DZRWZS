# 程序打包指南

## 🚀 如何将程序打包为EXE文件

### 方法一：使用.NET编译命令（推荐）

#### 步骤1：确保程序已修改完成
```bash
# 进入项目目录
cd "D:\cunchu\项目\电子任务助手\DiaryApp"
```

#### 步骤2：清理旧文件
```bash
dotnet clean
```

#### 步骤3：编译Release版本
```bash
dotnet build --configuration Release
```

#### 步骤4：查找生成的EXE文件
- **Debug版本**：`D:\cunchu\项目\电子任务助手\DiaryApp\bin\Debug\net8.0-windows\DiaryApp.exe`
- **Release版本**：`D:\cunchu\项目\电子任务助手\DiaryApp\bin\Release\net8.0-windows\DiaryApp.exe`

### 方法二：使用自动打包脚本

运行Python启动器，它会自动处理编译过程：
```bash
python "D:\cunchu\项目\电子任务助手\启动日记应用.py"
```

### 📁 文件结构说明

编译后会生成以下文件：
- `DiaryApp.exe` - 主程序文件
- `DiaryApp.dll` - 依赖库文件
- `DiaryApp.deps.json` - 依赖配置
- `DiaryApp.runtimeconfig.json` - 运行时配置

### 🔧 版本信息说明

版本信息会在运行时自动更新为当前时间：
- **版本号**：需要手动修改 `MainWindow.xaml.cs` 中的 `AppVersion.VERSION`
- **构建日期**：自动更新为当前日期
- **构建时间**：自动更新为当前时间

### ⚡ 快速打包流程

1. **修改代码** → 2. **运行版本更新脚本** → 3. **编译Release版本** → 4. **测试运行**

### 📋 注意事项

- Release版本比Debug版本更优化，启动更快
- 确保在编译前停止正在运行的程序
- 编译后的EXE文件需要包含所有依赖文件才能正常运行
- 建议使用Release版本作为最终发布版本