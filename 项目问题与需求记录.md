# 项目问题与需求记录

最后更新：2026-04-20

## 目的

用于记录已经处理过的问题、暂缓处理的需求、验证状态与后续修改方向，避免重复改动和反复犯错。

## 记录原则

- 已由用户手动处理的数据内容，不再由程序侧重复清理或回填。
- 已确认正常的功能先标记为“无误”，后续不重复改动。
- 暂缓需求必须保留原始意图和后续方向，避免下次沟通丢失上下文。
- 出现乱码、串色、兼容性之类问题时，优先记录“现象 + 原因范围 + 修改方向”。

## 当前条目

### 1. 六大板块鼠标悬停串色

- 状态：已解决
- 说明：鼠标靠近一个模块时，其他模块按钮误变色的问题已修复。
- 后续：除非再次复现，否则不再改动。

### 2. 个人记录清理与示例数据

- 状态：用户已手动处理
- 说明：用户已经自行删除个人记录并手动添加替代内容。
- 约束：后续不要再自动改写这部分现有数据，避免覆盖用户当前整理结果。

### 3. 新增日记界面的“今日概况”

- 状态：本轮处理
- 目标：改成可折叠形式，并默认折叠，方便优先输入日记正文。
- 修改方向：使用可展开面板承载“今日完成任务 / 时间记录饼状图 / 今日打卡”三块内容，默认收起。

### 4. 临时任务提醒时间

- 状态：暂缓
- 说明：以后再提，本轮不处理。
- 原始意图：支持设置“几月几日几点”提醒，理想情况下可推送到手机。
- 后续方向：先做桌面端准确提醒，再评估手机推送接入方式与成本。

### 5. 任务循环提醒

- 状态：暂缓
- 说明：以后再提，本轮不处理。
- 原始意图：例如每三天提醒一次，并通过“设置提醒”按钮配置。
- 后续方向：优先核对提醒规则计算、起始日期、下次触发时间与重复生成逻辑。

### 6. 时间记录编辑窗口长度

- 状态：无误
- 说明：当前确认没有问题，先不继续调整。

### 7. 标签颜色固定

- 状态：无误
- 说明：同一标签颜色保持一致的问题当前已正常。

### 8. 打卡统计中“今日打卡”按钮位置

- 状态：需求记录
- 说明：之前调整后出现异常，需要改回。
- 后续方向：恢复到用户更认可的原布局，避免为了靠近“连续打卡”而破坏现有排版。

### 9. 数据导入导出兼容性

- 状态：待验证
- 说明：当前“无法查看”，改日验证。
- 原始目标：新版本支持旧版本导出记录，并为未来版本兼容保留扩展空间。
- 后续方向：验证导入导出结构、版本字段、旧数据回退逻辑与异常提示。

### 10. 乱码问题

- 状态：已记录，需长期避免复发
- 现象：窗口文案、提示语、源码注释、运行时调试文本中曾出现乱码。
- 已处理范围：主窗口、设置窗口、提醒设置窗口、时间记录编辑窗口、日记编辑窗口等主要用户可见区域已清理。
- 风险点：文件编码不一致、旧字符串残留、复制粘贴异常文本。
- 后续方向：
  - 统一使用 UTF-8 保存源码与文案文件。
  - 修改 UI 文案时优先检查 XAML 与 code-behind 两侧是否同步。
  - 提交前搜索常见乱码片段，避免再次进入仓库。

### 11. 个人信息编辑按钮“点不到”

- 状态：本轮处理
- 现象：进入编辑态后，“编辑个人信息”按钮被直接禁用，视觉上像失效按钮，容易被误认为点不到或坏掉了。
- 修改方向：
  - 改成明确的编辑态切换按钮。
  - 编辑中显示“取消编辑”，而不是直接禁用。
  - 按钮宽度固定，增加可点击区域和状态辨识度。

### 12. “保存个人信息”按钮默认不可点击

- 状态：本轮处理
- 现象：保存按钮默认被禁用，用户在未理解“先编辑再保存”的前提下，会误认为按钮失效或点不到。
- 修改方向：
  - 保存按钮保持始终可点击。
  - 如果当前不在编辑状态，点击后给出明确提示，而不是直接禁用。
  - 保持“编辑”和“保存”的行为区分清晰，避免按钮看起来像坏掉。

### 13. 个人信息区按钮命中异常

- 状态：本轮处理
- 现象：按钮视觉上显示正常，但实际点击命中异常，用户反馈“这个按钮点不到”。
- 原因范围：个人信息区 `Grid` 行定义数量不足，`最后更新` 文本落在不存在的行号上，按钮区域又放在可拉伸行中，存在布局和命中区域错位风险。
- 修改方向：
  - “最后更新”使用独立 `Auto` 行。
  - 按钮区域使用独立 `Auto` 行。
  - 提高按钮容器层级，避免被邻近布局遮挡。

### 14. 打卡项目删除功能

- 状态：本轮处理
- 需求：支持删除打卡项目，但删除前必须明确提示确认。
- 修改方向：
  - 在打卡项目操作区增加“删除项目”按钮。
  - 删除前弹出确认框。
  - 删除项目时同步删除关联打卡记录，避免产生孤立数据。
  - 删除后刷新项目列表与右侧项目展示区域。

## 本轮不要动的内容

- 用户已经手动清理并补充的个人记录数据。
- 第 4、5 条提醒功能，先只保留需求，不在本轮继续开发。
- 第 8、9 条先记录状态，等用户后续确认再动。

## 长期规划

### 15. AI 语音输入与自然语言操作

- 状态：未来规划
- 目标：支持通过语音或自然语言快速录入内容，例如“加一个任务”“写一条日记”“记录今天学习两小时”等，减少手动输入负担。
- 初步方向：
  - 先做桌面端的语音转文字输入。
  - 再做自然语言意图识别，将一句话解析成任务、日记、时间记录、打卡等具体动作。
  - 保留手动确认步骤，避免 AI 误识别后直接写入错误内容。

### 16. 与智能硬件联动

- 状态：未来规划
- 目标：后续尝试和智能硬件结合，让提醒、打卡、状态同步或数据采集更方便。
- 初步方向：
  - 先明确要接入的硬件类型，例如手机通知、手表、音箱或其他设备。
  - 优先选择已有开放接口、接入成本较低的设备生态。
  - 接入前先整理本地事件与数据结构，避免以后硬件联动时重复改底层逻辑。

### 17. 手机版与 PC 同步

- 状态：未来规划
- 目标：未来增加手机版本，并与 PC 端实现数据同步，让这个工具可以跨设备持续使用。
- 初步方向：
  - 先把桌面端数据结构、导入导出格式、版本兼容机制整理稳定。
  - 后续再设计同步方案，例如本地局域网同步、云端同步或手动导入导出同步。
  - 手机版开发前，要先明确哪些核心功能必须优先保留：任务、日记、时间记录、打卡、提醒。
